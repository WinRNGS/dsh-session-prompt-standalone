# 统一用 LF 入库/检出：避免 Windows 上 clone 后 scripts/build.sh 变成 CRLF
# 而无法直接 `./scripts/build.sh` 执行（用 `bash scripts/build.sh` 调用不受影响）。
* text=auto eol=lf
*.sh text eol=lf
*.png binary
*.jpg binary
*.tgz binary
